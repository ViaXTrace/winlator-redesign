<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_main_gradient">

    <!-- Background glow orbs -->
    <View
        android:id="@+id/view_glow_top"
        android:layout_width="280dp"
        android:layout_height="280dp"
        android:background="#0800E5FF"
        android:alpha="0"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="-80dp"
        android:layout_marginEnd="-80dp" />

    <View
        android:id="@+id/view_glow_bottom"
        android:layout_width="260dp"
        android:layout_height="260dp"
        android:background="#087C4DFF"
        android:alpha="0"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginBottom="-60dp"
        android:layout_marginStart="-60dp" />

    <!-- Logo container -->
    <LinearLayout
        android:id="@+id/layout_logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:alpha="0"
        android:scaleX="0.7"
        android:scaleY="0.7"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/layout_tagline"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Hexagon glow ring -->
        <FrameLayout
            android:layout_width="140dp"
            android:layout_height="140dp">
            <View
                android:layout_width="140dp"
                android:layout_height="140dp"
                android:background="#1400E5FF"
                android:layout_gravity="center" />
            <ImageView
                android:id="@+id/iv_logo"
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:src="@drawable/ic_app_logo"
                android:layout_gravity="center"
                android:contentDescription="@string/app_name" />
        </FrameLayout>

        <TextView
            android:id="@+id/tv_app_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="@color/color_text_primary"
            android:textSize="36sp"
            android:fontFamily="sans-serif-light"
            android:letterSpacing="0.15"
            android:layout_marginTop="20dp" />

    </LinearLayout>

    <!-- Tagline -->
    <LinearLayout
        android:id="@+id/layout_tagline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:alpha="0"
        app:layout_constraintTop_toBottomOf="@id/layout_logo"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.15">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Windows Compatibility Layer"
            android:textColor="@color/color_text_secondary"
            android:textSize="14sp"
            android:letterSpacing="0.1" />

        <View
            android:layout_width="60dp"
            android:layout_height="1dp"
            android:background="@color/color_accent_cyan"
            android:layout_gravity="center"
            android:layout_marginTop="16dp"
            android:alpha="0.5" />
    </LinearLayout>

    <!-- Loading indicator -->
    <LinearLayout
        android:id="@+id/layout_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:alpha="0"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="60dp">

        <ProgressBar
            android:id="@+id/progress_loading"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:indeterminateTint="@color/color_accent_cyan" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Initializing…"
            android:textColor="@color/color_text_tertiary"
            android:textSize="12sp"
            android:layout_marginStart="10dp"
            android:letterSpacing="0.05" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
